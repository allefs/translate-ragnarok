# Translate Ragnarok

## Instructions to Run via Docker
Run the following command to start the services:
```bash
docker compose up --build
```

## Local Development Instructions
To run the project locally, set up your environment with the required dependencies and run the appropriate commands for both the API and web services.